// HelloWorld.cpp
// THis is a preprocesor directive
#include <iostream>

// This is the main function.
// argv[0] - represent the program name. NOTE(campeanu): Never use it in practice.
int main(int argc, char* argv[])
{
    // This is how C++ print text on the screen. 
    std::cout << "Hello world!" << std::endl;
    return 0;
}